import{D as e,F as t,O as n,f as r}from"./app-uaayYDl-.js";var i=JSON.parse(`{"path":"/ai/agent/pi/","title":"Pi介绍","lang":"zh-CN","frontmatter":{"title":"Pi介绍","description":"开源命令行编码代理 Pi：安装、配置、资源加载机制、Skills、子代理、快捷键与使用命令","tags":["AI","AI Agent","Pi"],"order":7},"git":{"updatedTime":1786615504000,"contributors":[{"name":"吴汉青","username":"","email":"554553400@qq.com","commits":1}],"changelog":[{"hash":"9cc0d3544769b8d7febd79bf60a0d93f8c86504d","time":1786615504000,"email":"554553400@qq.com","author":"吴汉青","message":"新增 Pi 智能体 README 文档：记录安装、配置、资源加载、Skills、子代理与使用命令"}]},"filePathRelative":"ai/agent/pi/README.md"}`),a={name:`README.md`};function o(r,i,a,o,s,c){return t(),e(`div`,null,[...i[0]||=[n(`<h2 id="简介" tabindex="-1"><a class="header-anchor" href="#简介"><span>简介</span></a></h2><p>Pi（<code>@earendil-works/pi-coding-agent</code>）是一个基于大语言模型的本机命令行编码代理，核心工作方式是&quot;单主会话 + 工具调用&quot;。通过读文件、跑命令、改代码等工具完成任务。</p><ul><li><p>开源主页：https://github.com/earendil-works/pi</p></li><li><p>运行环境：Node.js（需预先安装）</p></li><li><p>单主代理模型：<strong>主代理 = 当前对话会话</strong>，没有多主代理并存</p></li><li><p>资源目录：<code>~/.pi/agent/</code>（即 <code>C:\\Users\\santic\\.pi\\agent\\</code>）</p></li></ul><h2 id="下载与安装" tabindex="-1"><a class="header-anchor" href="#下载与安装"><span>下载与安装</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># 通过 npm 全局安装</span></span>
<span class="line"><span class="token function">npm</span> <span class="token function">install</span> <span class="token parameter variable">-g</span> --ignore-scripts @earendil-works/pi-coding-agent</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 验证</span></span>
<span class="line">pi <span class="token parameter variable">--version</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="配置" tabindex="-1"><a class="header-anchor" href="#配置"><span>配置</span></a></h2><p>核心配置文件为 <code>~/.pi/agent/settings.json</code>，项目级配置为 <code>.pi/settings.json</code>（覆盖全局、嵌套合并）。登录凭证存 <code>~/.pi/agent/auth.json</code>，用 <code>/login</code> 管理。</p><p>示例：</p><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;defaultProvider&quot;</span><span class="token operator">:</span> <span class="token string">&quot;deepseek&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;defaultModel&quot;</span><span class="token operator">:</span> <span class="token string">&quot;deepseek-v4-flash&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;defaultThinkingLevel&quot;</span><span class="token operator">:</span> <span class="token string">&quot;off&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;theme&quot;</span><span class="token operator">:</span> <span class="token string">&quot;dark&quot;</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;skills&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&quot;D:/projects/ai/aiteams/skills&quot;</span><span class="token punctuation">]</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;prompts&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;extensions&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token property">&quot;packages&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>项</th><th>作用</th></tr></thead><tbody><tr><td><code>defaultProvider</code> / <code>defaultModel</code></td><td>默认模型</td></tr><tr><td><code>defaultThinkingLevel</code></td><td>默认思考等级</td></tr><tr><td><code>theme</code></td><td>主题</td></tr><tr><td><code>skills</code></td><td>技能目录/文件数组（支持 glob 和排除）</td></tr><tr><td><code>prompts</code></td><td>模板目录/文件数组</td></tr><tr><td><code>extensions</code></td><td>扩展文件/目录数组</td></tr><tr><td><code>packages</code></td><td>npm/git 资源包</td></tr></tbody></table><p>数组支持：<code>!pattern</code> 排除、<code>+path</code> 强制包含、<code>-path</code> 排除。</p><h3 id="思考等级" tabindex="-1"><a class="header-anchor" href="#思考等级"><span>思考等级</span></a></h3><ul><li>快捷键 <code>Shift+Tab</code> 循环切换思考等级（off → minimal → low → medium → high → xhigh）</li><li>可在配置中按模型映射（<code>models.&lt;name&gt;.thinkingLevelMap</code>），<code>null</code> 表示该等级不支持、从 UI 隐藏：</li></ul><div class="language-json line-numbers-mode" data-highlighter="prismjs" data-ext="json"><pre><code class="language-json"><span class="line"><span class="token punctuation">{</span></span>
<span class="line">  <span class="token property">&quot;models&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token property">&quot;deepseek-v4-flash&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token property">&quot;thinkingLevelMap&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token property">&quot;minimal&quot;</span><span class="token operator">:</span> <span class="token null keyword">null</span><span class="token punctuation">,</span> <span class="token property">&quot;low&quot;</span><span class="token operator">:</span> <span class="token null keyword">null</span><span class="token punctuation">,</span> <span class="token property">&quot;medium&quot;</span><span class="token operator">:</span> <span class="token null keyword">null</span><span class="token punctuation">,</span></span>
<span class="line">        <span class="token property">&quot;high&quot;</span><span class="token operator">:</span> <span class="token string">&quot;high&quot;</span><span class="token punctuation">,</span> <span class="token property">&quot;xhigh&quot;</span><span class="token operator">:</span> <span class="token string">&quot;max&quot;</span></span>
<span class="line">      <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="资源加载机制" tabindex="-1"><a class="header-anchor" href="#资源加载机制"><span>资源加载机制</span></a></h2><p>Pi 从约定目录加载资源，规则<strong>逐资源不同、并不统一递归</strong>：</p><table><thead><tr><th>资源</th><th>目录</th><th>发现规则</th></tr></thead><tbody><tr><td>Skills 技能</td><td><code>~/.pi/agent/skills/</code>、项目 <code>.pi/skills/</code></td><td><strong>递归</strong>（含 SKILL.md 的子目录）+ 根目录零散 .md</td></tr><tr><td>Prompts 模板</td><td><code>~/.pi/agent/prompts/</code>、项目 <code>.pi/prompts/</code></td><td><strong>仅根目录 .md，不递归</strong></td></tr><tr><td>Agents 子角色</td><td><code>~/.pi/agent/agents/</code></td><td><strong>仅根目录 .md，不递归</strong></td></tr><tr><td>Extensions 扩展</td><td><code>~/.pi/agent/extensions/</code></td><td><code>*.ts</code> 或 <code>*/index.ts</code></td></tr><tr><td>上下文</td><td>从 cwd 向上回溯</td><td>各级 AGENTS.md / CLAUDE.md</td></tr></tbody></table><h2 id="skills-技能" tabindex="-1"><a class="header-anchor" href="#skills-技能"><span>Skills 技能</span></a></h2><p>技能是独立的&quot;按需加载的工作包&quot;（SKILL.md + 脚本 + 参考文档目录）。</p><p><strong>渐进式披露机制</strong>：</p><ol><li>启动时 Pi 只把技能的<strong>名称 + description</strong> 放进系统提示词</li><li>模型判断&quot;需要时&quot;才用 <code>read</code> 载入完整 SKILL.md 指令</li><li>完整指令不常驻上下文，按需读取</li></ol><p>因此<strong>触发是否准确取决于 description 写得够不够具体</strong>。缺 description 的技能不加载。触发方式：模型自行判断，或 <code>/skill:my-skill</code> 强制。</p><p>自动发现目录：<code>~/.pi/agent/skills/</code>、<code>~/.agents/skills/</code>、项目 <code>.pi/skills/</code>、<code>.agents/skills/</code>。也可通过 <code>settings.json</code> 的 <code>skills</code> 字段直接指向外部目录（推荐用于 Git 管理）——<strong>是&quot;Git 单一来源&quot;的正解，不需要软链、不需要复制</strong>。</p><h2 id="prompts-提示模板" tabindex="-1"><a class="header-anchor" href="#prompts-提示模板"><span>Prompts 提示模板</span></a></h2><p>提示模板用 <code>/名字</code> 触发，注入上下文片段。文件名即命令名，如 <code>review.md</code> → <code>/review</code>，输入框敲 <code>/</code> 自动补全。</p><ul><li>只扫描根目录 <code>.md</code>，子目录不递归（放子目录不会生效）</li><li>仅注入当前会话上下文，不另起子进程</li><li>支持 <code>$1/$2/$@</code> 位置参数</li></ul><p>配置：<code>settings.json</code> 的 <code>prompts</code> 字段指向外部目录（如 <code>[&quot;D:/projects/ai/aiteams/prompts&quot;]</code>）。</p><h2 id="extensions-扩展" tabindex="-1"><a class="header-anchor" href="#extensions-扩展"><span>Extensions 扩展</span></a></h2><p>扩展是 TypeScript 模块，能在事件钩子上拦截/改造行为、注册自定义工具和命令。典型用途：权限门（危险命令确认）、子代理编排、自定义工具/命令/快捷键、路径保护、git 检查点、自定义压缩。</p><ul><li>目录：<code>~/.pi/agent/extensions/*.ts</code>（全局）或项目 <code>.pi/extensions/*.ts</code></li><li>调试：<code>pi -e ./path.ts</code></li><li>重载：<code>/reload</code>（扩展放自动发现目录可热重载）</li><li>扩展运行在用户完整权限下，<strong>只装可信来源</strong></li></ul><h2 id="子代理-agents" tabindex="-1"><a class="header-anchor" href="#子代理-agents"><span>子代理（Agents）</span></a></h2><p>Pi 代理模型是<strong>单主 + 扁平子进程</strong>，不是多主并存。角色是常驻的&quot;定义/简历&quot;（存于 <code>~/.pi/agent/agents/*.md</code>，带 <code>name / description / tools / model</code> frontmatter），进程是每次按需新开的独立 <code>pi</code> 子进程，用完即走、无状态。</p><p>三种调用模式：</p><ul><li>单任务 <code>{ agent, task }</code>：一个角色干一件事</li><li>并行 <code>{ tasks: [...] }</code>：多角色并发（上限 8 任务 / 4 并发）</li><li>链式 <code>{ chain: [...] }</code>：顺序流水线，<code>{previous}</code> 占位符传上一步输出</li></ul><p>子代理特性：隔离上下文、一次性无状态、可观测（工具链、过程文本、用量/成本可见，内部思考草稿不可见）。</p><p>官方 subagent 扩展要求角色<strong>预先定义</strong>（按名字查 <code>agents/*.md</code>），找不到报错。变通：定义&quot;通用下属角色&quot;，把每轮定向指令通过 task 参数临时注入，效果等同&quot;实时新建聚焦角色&quot;。</p><h2 id="权限模式与安全" tabindex="-1"><a class="header-anchor" href="#权限模式与安全"><span>权限模式与安全</span></a></h2><p>Pi 没有内置的&quot;只读/询问/完全放开&quot;三级权限开关（官方定位：&quot;No permission popups&quot;）。靠两套机制实现：</p><ol><li><strong>启动参数裁剪工具（等效权限档）</strong>：</li></ol><table><thead><tr><th>想要的模式</th><th>参数</th></tr></thead><tbody><tr><td>只读（无工具）</td><td><code>--no-tools</code> / <code>-nt</code></td></tr><tr><td>只读 + 少量工具</td><td><code>--no-builtin-tools</code> / <code>-nbt</code> 或 <code>--tools read,ls,grep</code></td></tr><tr><td>白名单</td><td><code>--tools &lt;列表&gt;</code></td></tr><tr><td>完全放开</td><td>默认（read/write/edit/bash）</td></tr></tbody></table><p>启动后不能在窗口内动态切换，需以不同参数开新会话。</p><ol start="2"><li><strong>扩展实现&quot;询问&quot;模式</strong>：用 <code>tool_call</code> 事件拦截 + <code>ctx.ui.confirm</code> 弹窗确认危险命令（参考 <code>examples/extensions/permission-gate.ts</code>）。</li></ol><h2 id="快捷键" tabindex="-1"><a class="header-anchor" href="#快捷键"><span>快捷键</span></a></h2><table><thead><tr><th>键</th><th>作用</th></tr></thead><tbody><tr><td><code>Shift+Tab</code></td><td>思考等级循环</td></tr><tr><td><code>Ctrl+T</code></td><td>折叠/展开思考块</td></tr><tr><td><code>Ctrl+L</code></td><td>模型选择器</td></tr><tr><td><code>Ctrl+P</code> / <code>Shift+Ctrl+P</code></td><td>下一个/上一个模型</td></tr><tr><td><code>Ctrl+O</code></td><td>展开折叠的工具输出</td></tr><tr><td><code>Ctrl+V</code>/<code>Alt+V</code>(Win)</td><td>粘贴图片</td></tr></tbody></table><h2 id="使用与常用命令" tabindex="-1"><a class="header-anchor" href="#使用与常用命令"><span>使用与常用命令</span></a></h2><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">pi                        <span class="token comment"># 启动交互</span></span>
<span class="line">pi <span class="token parameter variable">-c</span>                     <span class="token comment"># 继续最近会话</span></span>
<span class="line">pi <span class="token parameter variable">-r</span>                     <span class="token comment"># 浏览历史会话</span></span>
<span class="line">pi @file.md <span class="token string">&quot;prompt&quot;</span>      <span class="token comment"># 关联文件</span></span>
<span class="line">pi <span class="token parameter variable">-p</span> <span class="token string">&quot;prompt&quot;</span>            <span class="token comment"># 非交互：处理即退出</span></span>
<span class="line">pi <span class="token parameter variable">-t</span> read,ls,grep <span class="token string">&quot;...&quot;</span>  <span class="token comment"># 白名单工具</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>会话内：</p><ul><li><code>/new</code> <code>/resume</code> <code>/tree</code> <code>/fork</code> <code>/clone</code> 管理会话树</li><li><code>/model</code> 切换模型</li><li><code>/reload</code> 重载资源（改 AGENTS.md / skills / 扩展后）</li><li><code>!command</code> 跑命令并纳入上下文；<code>!!command</code> 跑命令不带入上下文</li></ul><h2 id="与其它-agent-框架的差异" tabindex="-1"><a class="header-anchor" href="#与其它-agent-框架的差异"><span>与其它 Agent 框架的差异</span></a></h2><p>Pi 与 autogen / crewai / metagpt 等&quot;多 Agent 框架&quot;不同：</p><ul><li>Pi 是<strong>单主代理 + 扁平子进程</strong>，不是&quot;多 Agent 相互对话/自动编排的角色网络&quot;</li><li>没有显式的角色继承树；&quot;多领域团队&quot;需用 chain/parallel 自行编排子代理</li><li>其他框架的专属 frontmatter 字段（<code>mode: primary</code>、<code>temperature</code>、<code>permission</code>、<code>runAs</code> 等）Pi 不解析</li></ul>`,51)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};